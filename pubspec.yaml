name: principle_utils
description: "A Dart package for Flutter that delivers responsive layouts, versatile string case conversions, color utilities, and type-safe patterns for robust app development."

version: 0.1.6+3

homepage: https://github.com/PrincipleConsulting/principle_utils
repository: https://github.com/PrincipleConsulting/principle_utils

environment:
  sdk: ^3.5.4
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4