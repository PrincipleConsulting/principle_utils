/// A utility class that holds commonly used border radius values.
abstract final class TWRadiusSize {
  /// Small radius of 4.0 logical pixels for subtle curves
  static const double small = 4.0;

  /// Medium radius of 8.0 logical pixels for moderate curves
  static const double medium = 8.0;

  /// Large radius of 12.0 logical pixels for pronounced curves
  static const double large = 12.0;

  /// Extra large radius of 24.0 logical pixels for high visual impact
  static const double extraLarge = 24.0;

  /// Circular radius of 50.0 logical pixels for full rounding
  static const double circular = 50.0;

  /// Pill-shaped radius of 100.0 logical pixels for extreme roundness
  static const double pill = 100.0;

  /// Minimal radius of 2.0 logical pixels for sharp edges
  static const double minimal = 2.0;

  /// Large radius of 36.0 logical pixels for big components
  static const double huge = 36.0;

  /// Very large radius of 48.0 logical pixels for cards/buttons
  static const double veryLarge = 48.0;

  /// Custom radius of 60.0 logical pixels for special cases
  static const double customRadius = 60.0;

  /// Ultra-large radius of 72.0 logical pixels for extreme curves
  static const double ultraLarge = 72.0;
}

/// A utility class that holds commonly used border width values.
abstract final class TWBorderWidth {
  /// Thin border of 1.0 logical pixel (default line width)
  static const double thin = 1.0;

  /// Standard border of 2.0 logical pixels (regular emphasis)
  static const double standard = 2.0;

  /// Thick border of 4.0 logical pixels for strong emphasis
  static const double thick = 4.0;

  /// Extra thick border of 6.0 logical pixels for heavy elements
  static const double extraThick = 6.0;

  /// Custom border of 8.0 logical pixels for unique needs
  static const double customWidth = 8.0;

  /// Very thin border of 0.5 logical pixels for subtle outlines
  static const double veryThin = 0.5;

  /// Wide border of 12.0 logical pixels for containers/cards
  static const double wide = 12.0;

  /// Heavy border of 16.0 logical pixels for warnings/alerts
  static const double heavy = 16.0;

  /// Extra wide border of 20.0 logical pixels for emphasis
  static const double extraWide = 20.0;

  /// Ultra-wide border of 24.0 logical pixels for max emphasis
  static const double ultraWide = 24.0;

  /// Super wide border of 30.0 logical pixels for special cases
  static const double superWide = 30.0;
}
